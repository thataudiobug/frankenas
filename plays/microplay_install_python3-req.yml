#installs the python3-requests dependancy on a host, default port_royal
- name: Install the thing
  hosts: port_royal

  tasks:
  - name: do the thing
    apt:
      name: python3-requests
      state: present
      update_cache: true
