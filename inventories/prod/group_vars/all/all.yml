---
# Fleet-wide defaults (apply to every host unless overridden).
#
# Role-specific catalogs moved into their owning roles' defaults in Phase 3:
#   * log_sources_catalog   -> roles/config_logging/defaults
#   * authelia_access_catalog -> roles/service_authelia/defaults
#
# Genuinely cross-cutting catalogs (users_catalog, api_catalog,
# droplet_bind_catalog) remain in group_vars because they're referenced from
# always-in-scope contexts and by multiple roles.
